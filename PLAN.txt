================================================================================
LAFDAMAP — MVP BUILD PLAN & PROGRESS LOG
================================================================================
Last updated: 2026-03-22 (UTC) — Live events + media iteration shipped
Source PRD: prd.txt (Product Requirements Document v1.0 MVP)

This file is the single project progress ledger. Cursor agents must keep it in
sync when phases ship (see .cursor/rules/lafdamap-plan-sync.mdc).

--------------------------------------------------------------------------------
1. PRODUCT SUMMARY
--------------------------------------------------------------------------------
LafdaMap: real-time, community-driven incident map (urban India, viral /
hyper-local). Users browse incidents on a map, report anonymously, and (later)
go live / watch streams per incident.

Locked scope decisions:
  - Geography: single-city MVP; default map center via env (e.g. Pune).
  - Incidents: no auto-expiry; moderation via Supabase Studio for now.

Out of scope for MVP (per PRD): named accounts, push notifications, upvotes,
SFU streaming, incident expiry automation, native apps.

--------------------------------------------------------------------------------
2. TECH STACK (AS IMPLEMENTED)
--------------------------------------------------------------------------------
  - Frontend: Next.js 14 (App Router), React 18, TypeScript, Tailwind CSS
  - UI: shadcn-style components (Button, tokens in app/globals.css)
  - Map: MapLibre GL via react-map-gl/maplibre + OpenFreeMap tiles (no API key)
  - Backend: Supabase (Postgres, Auth, Realtime)
  - Auth: Supabase anonymous sign-in (signInAnonymously), @supabase/ssr
  - Live streaming (Phase 5): WebRTC via simple-peer + Supabase Realtime broadcast signaling; STUN via env (TURN optional)
  - PWA (Phase 6): `app/manifest.ts` + `public/icon.svg` (installable; no offline service worker)
  - Storage: Supabase Storage bucket `incident-media` + `incident_media` + upload/record on incident detail (reporter-only)

Environment variables (see .env.example):
  - NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY
  - NEXT_PUBLIC_MAP_STYLE_URL (optional; default OpenFreeMap dark)
  - NEXT_PUBLIC_MAP_DEFAULT_CENTER (lat,lng,zoom)
  - NEXT_PUBLIC_SITE_URL (optional; Open Graph / canonical for /incident/[id])
  - NEXT_PUBLIC_WEBRTC_ICE_SERVERS (optional; comma-separated STUN/TURN URLs)

--------------------------------------------------------------------------------
3. PHASE OVERVIEW (STATUS)
--------------------------------------------------------------------------------
  Phase 1 — Foundation          [DONE]
  Phase 2 — Map + data          [DONE]
  Phase 3 — Report incident     [DONE]
  Phase 4 — Detail + share      [DONE]
  Phase 5 — Live WebRTC + chat  [DONE]
  Phase 6 — Ship (PWA, Vercel)  [DONE]
  Phase 7 — Live hardening + events + media  [DONE]

Dependency order: 1 → 2 → 3 → 4 → 5 → 6 → 7

--------------------------------------------------------------------------------
4. DETAILED TODO LIST — COMPLETED
--------------------------------------------------------------------------------

PHASE 1 — Foundation [DONE]
  [x] Scaffold Next.js 14 + TypeScript + Tailwind + ESLint
  [x] shadcn-style UI baseline (components/ui/button, lib/utils, globals theme)
  [x] Supabase browser client (lib/supabase/client.ts) + server helper
      (lib/supabase/server.ts)
  [x] AuthProvider: silent signInAnonymously when Supabase env is set
  [x] Map: MapLibre + OpenFreeMap (lib/map-style.ts, lib/map-config.ts)
  [x] Dynamic map load on home page (ssr: false)
  [x] .env.example + README setup notes
  [x] next.config transpilePackages for maplibre-gl

PHASE 2 — Map + data [DONE]
  [x] SQL migration: public.incidents + constraints + indexes
      File: supabase/migrations/20250322120000_incidents.sql
  [x] RLS: SELECT for anon + authenticated; INSERT/UPDATE/DELETE own rows
  [x] Realtime: incidents in supabase_realtime publication; replica identity full
  [x] Types + parsing: lib/incidents.ts (types, colors, parseIncidentRow)
  [x] Hook: hooks/use-incidents-realtime.ts (initial fetch + postgres_changes)
  [x] UI: components/map/lafda-map.tsx (map shell, navigation control)
  [x] Filter chips: All | Street Fight | Road Rage | JCB Khudai | Chaos
  [x] Incident markers with PRD colors; pulse when is_live
  [x] User GPS dot + flyTo when geolocation succeeds
  [x] README: migration + test insert SQL
  [x] Plan marker: Phase 2 completed in Cursor plan file

PHASE 3 — Report incident [DONE]
  [x] Floating primary “Report incident” control (center-bottom FAB)
  [x] Bottom sheet: Step 1 — 2×2 category grid (emoji cards, PRD colors)
  [x] Step 2 — title (max 80), optional description (max 300)
  [x] Location on submit via navigator.geolocation; fallback map center → default city
  [x] Brief success copy when fallback location used; insert user_id = session user (RLS)
  [x] New pins via existing Realtime subscription (no hook change)

PHASE 4 — Detail + share [DONE]
  [x] Tap marker → bottom sheet detail card (IncidentDetailSheet)
  [x] Category badge, title, description, relative time, reporter handle
  [x] Reporter handle: anonymous_XXXX from auth user_id (deterministic last 4 hex)
  [x] “Watch live” / “Go live here” disabled + title (Phase 5)
  [x] Share (Web Share API or copy) + /incident/[id] page + ?incident= deep link to map
  [x] Open Graph + Twitter metadata + canonical via getPublicSiteUrl()

PHASE 5 — Live WebRTC + chat [DONE]
  [x] DB: live_sessions + partial unique (one active session / incident) + triggers → incidents.is_live
  [x] RLS: public read; host insert/update own; viewer_count via live_session_viewer_delta RPC
  [x] Signaling: Realtime broadcast on live:{sessionId} (SDP/ICE, multi-viewer host mesh)
  [x] simple-peer host (initiator) per viewer; STUN defaults + optional NEXT_PUBLIC_WEBRTC_ICE_SERVERS
  [x] LiveRoom HUD: LIVE, category, presence-based viewer count, end/leave
  [x] Live chat via broadcast (Phase 7 adds Postgres persistence + history)
  [x] End stream: host updates session + broadcast; trigger clears is_live / pin pulse

PHASE 6 — Ship [DONE]
  [x] PWA: `app/manifest.ts`, `public/icon.svg`, standalone display, viewport/theme
  [x] Mobile-first tweaks: filter chip row scroll, safe-area FAB, touch-manipulation, tap highlight off
  [x] README: Vercel import, env vars, smoke test table
  [x] PLAN.txt / README aligned with stack

PHASE 7 — Live hardening + events + media [DONE]
  [x] Reporter-only live: `start_live_session_for_incident` RPC + revoke direct INSERT on `live_sessions`
   (supabase/migrations/20250322150000_reporter_only_live.sql); lib/live-sessions.ts; UI gating
  [x] LiveRoom: camera flip + overscroll/touch mitigations; `endLiveSession` filters host
  [x] Persisted chat: `live_chat_messages` + optional `peak_viewers` (20250322160000_live_chat_peak.sql);
   LiveRoom inserts + broadcast; public `/events` + incident history sections
  [x] Media: bucket `incident-media` + `incident_media` (20250322170000_incident_media.sql);
   upload + MediaRecorder on `/incident/[id]` (IncidentMediaSection)

--------------------------------------------------------------------------------
5. DETAILED TODO LIST — REMAINING
--------------------------------------------------------------------------------

(MVP phases 1–6 complete; Phase 7 iteration shipped. Further work is product iteration, not tracked here.)

--------------------------------------------------------------------------------
6. PRD FEATURE MAPPING (F1–F6)
--------------------------------------------------------------------------------
  F1 Incident map      — Phases 1–2 (live pulse already for is_live)
  F2 Detail card       — Phase 4 (+ live CTAs Phase 5)
  F3 Report flow       — Phase 3
  F4 Live streaming    — Phase 5 (+ Phase 7 reporter-only + persisted chat)
  F5 Guest auth        — Phase 1 (ongoing)
  F6 Media on incident — Phase 7 (Storage + incident_media)

--------------------------------------------------------------------------------
7. KEY FILES (REFERENCE)
--------------------------------------------------------------------------------
  prd.txt
  PLAN.txt                          (this file)
  .env.example
  README.md
  app/page.tsx
  app/manifest.ts
  app/incident/[id]/page.tsx
  app/events/page.tsx
  app/layout.tsx
  public/icon.svg
  app/globals.css
  components/providers/auth-provider.tsx
  components/live/live-room.tsx
  components/incident/incident-page-live.tsx
  components/incident/incident-history-section.tsx
  components/incident/incident-media-section.tsx
  components/map/lafda-map.tsx
  components/map/incident-detail-sheet.tsx
  components/map/incident-detail-card.tsx
  components/map/report-incident-sheet.tsx
  components/map/filter-chips.tsx
  components/map/incident-marker.tsx
  hooks/use-incidents-realtime.ts
  hooks/use-live-session-meta.ts
  hooks/use-open-live-room.ts
  lib/incidents.ts
  lib/live-sessions.ts
  lib/live-chat.ts
  lib/get-incident-history.ts
  lib/get-incident-media.ts
  lib/list-incidents.ts
  lib/webrtc-config.ts
  lib/get-incident.ts
  lib/relative-time.ts
  lib/site-url.ts
  lib/map-config.ts
  lib/map-style.ts
  lib/supabase/client.ts
  lib/supabase/server.ts
  supabase/migrations/20250322120000_incidents.sql
  supabase/migrations/20250322140000_live_sessions.sql
  supabase/migrations/20250322150000_reporter_only_live.sql
  supabase/migrations/20250322160000_live_chat_peak.sql
  supabase/migrations/20250322170000_incident_media.sql
  .cursor/rules/lafdamap-plan-sync.mdc

--------------------------------------------------------------------------------
8. NOTES FOR AGENTS
--------------------------------------------------------------------------------
  - After shipping a phase: mark section 3 status, flip [ ] to [x] in section 5,
    add bullets under section 4 if you split tasks, bump “Last updated” date.
  - When adding migrations, list new files in section 7.
  - Stack doc in section 2 should reflect reality (e.g. MapLibre vs Mapbox).

================================================================================
END OF PLAN
================================================================================
